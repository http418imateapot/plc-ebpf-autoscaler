name: SQL Syntax

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sql-lint:
    name: SQL Parse Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install sqlfluff
        run: pip install sqlfluff==3.3.1

      - name: Parse SQL files
        run: |
          sql_files=$(find . -name "*.sql" -not -path "./.git/*" | sort)
          if [ -z "$sql_files" ]; then
            echo "No SQL files found — nothing to lint."
            exit 0
          fi
          echo "Parsing SQL files:"
          echo "$sql_files"
          sqlfluff parse --dialect sqlite $sql_files
